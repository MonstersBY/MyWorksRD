import WOW from 'wow.js';

const wow = new WOW({
  boxClass: 'wow', // default
  animateClass: 'animate__animated', // default
  offset: 170, // default
  mobile: true, // default
  live: true // default
});
wow.init();
